apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "plat-ng-test.fullname" . }}-test-connection"
  labels:
    {{- include "plat-ng-test.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "plat-ng-test.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
